export enum TaskActionEnum {
  JOIN = 'JOIN',
  FORWARD = 'FORWARD',
  CHECK = 'CHECK',
  COPY = 'COPY',
  INVITE = 'INVITE'
}


export enum TaskTypeEnum {
  JOIN = 'JOIN',
  INVITE = 'INVITE'
}

export enum TaskAwardTypeEnum {
  MAX_ENERGY = 'MAX_ENERGY'
}

export enum TaskStatusEnum {
  COMPLETED = 'COMPLETED',
  ANY = 'ANY'
}
